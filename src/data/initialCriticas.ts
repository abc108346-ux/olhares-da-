import { Critica } from '../types';

/**
 * Initial dataset for Olhares da Cena.
 * Initialized as an empty catalog as requested by the user.
 * Critiques can be created, edited, and published directly through the /admin portal.
 */
export const INITIAL_CRITICAS: Critica[] = [];
